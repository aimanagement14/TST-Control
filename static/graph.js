/* Todo Sobre Todo — Entry point del editor de grafo y del runner.

   El modulo principal vive en ./graph/index.js. Este archivo solo
   re-exporta para mantener compatibilidad con el <script src="...">
   de las plantillas, que ya apunta a /static/graph.js.
*/

import './graph/index.js';
